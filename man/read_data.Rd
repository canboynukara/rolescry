% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_data.R
\name{read_data}
\alias{read_data}
\title{Read a data file with automatic header detection}
\usage{
read_data(
  path,
  header = NULL,
  sheet = NULL,
  na_strings = c("", "NA", "N/A", "n/a", "na", "NULL", "null", "."),
  verbose = FALSE
)
}
\arguments{
\item{path}{Path to the file.}

\item{header}{Optional integer giving the 1-based header row to use directly,
bypassing detection. \code{NULL} (default) auto-detects.}

\item{sheet}{Optional sheet name/index for Excel files.}

\item{na_strings}{Character vector of tokens mapped to \code{NA} before type
conversion.}

\item{verbose}{Logical; emit the detected header row via \code{message()}.}
}
\value{
A \code{data.frame} with detected column names and per-column types
inferred via \code{\link[utils]{type.convert}}.
}
\description{
Reads a tabular file into a \code{data.frame}, detecting the header row with
\code{\link[=detect_header]{detect_header()}} (the data is first read with no header so the header row is
visible as data). Delimited text (.csv/.tsv) is read with base R and always
works; spreadsheet and statistical formats use optional packages and degrade
gracefully with an actionable error if the package is absent.
}
\details{
Supported: \code{.csv}, \code{.tsv}/\code{.tab} (base);
\code{.xlsx}/\code{.xls}/\code{.xlsm} (Suggests: readxl or openxlsx);
\code{.sav}/\code{.sas7bdat}/\code{.dta} (Suggests: haven, header intrinsic);
\code{.rds} (base, returned as stored).
}
\examples{
tmp <- tempfile(fileext = ".csv")
writeLines(c("age,sex,score", "34,M,8.1", "51,F,7.4"), tmp)
read_data(tmp)
file.remove(tmp)

}
\seealso{
\code{\link[=detect_header]{detect_header()}}, \code{\link[=detect_roles]{detect_roles()}}
}
